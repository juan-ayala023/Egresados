# URL base del backend, sin barra final.
# El tunel cambia cada vez que el back reinicia: pidele la URL nueva y
# actualiza tu .env.local (este archivo es solo la plantilla del repo).
NEXT_PUBLIC_API_URL=https://j43mdnh9-4000.use.devtunnels.ms
