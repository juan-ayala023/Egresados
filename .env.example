# URL base del backend, sin barra final.
# El tunel cambia cada vez que el back reinicia: pidele la URL nueva y
# actualiza tu .env.local (este archivo es solo la plantilla del repo).
NEXT_PUBLIC_API_URL=https://j43mdnh9-4000.use.devtunnels.ms

# URL publica del SITIO, sin barra final. El colegio asigno este subdominio el
# 13 de septiembre de 2026.
#
# Se usa para dos cosas que NO pueden depender de desde donde se abrio la
# pagina: el enlace que se comparte por WhatsApp (si no, desde Vercel o desde
# un tunel se compartiria esa URL y no la oficial) y la vista previa que
# WhatsApp/Facebook arman al pegar el enlace (metadataBase).
NEXT_PUBLIC_SITIO_URL=https://homecomingtcs.columbus.edu.co
